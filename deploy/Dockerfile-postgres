FROM postgres:10.13

RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
    \
    \
    libgnutls30 \
    libperl5.24 \
    perl \
    perl-base \
    perl-modules-5.24 \
  && rm -rf /var/lib/apt/lists/*
