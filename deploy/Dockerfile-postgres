FROM postgres:10.9

# exim4 packages for https://security-tracker.debian.org/tracker/CVE-2019-13917

RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
    \
    exim4 \
    exim4-config \
    exim4-daemon-light \
  && rm -rf /var/lib/apt/lists/*
