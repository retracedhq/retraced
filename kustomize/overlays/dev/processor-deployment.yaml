apiVersion: apps/v1
kind: Deployment
metadata:
  name: retraced-processor
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: processor
          command:
            - node
            - --inspect=0.0.0.0
            - build/src/_processor/index.js
#          ports:
#          - containerPort: 9229
