apiVersion: apps/v1
kind: Deployment
metadata:
  name: jackson-admin-portal
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: jackson
        - image: boxyhq/jackson:1.6.0
          envFrom:
            - secretRef:
              name: auditlog
