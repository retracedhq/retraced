apiVersion: v1
kind: Pod
metadata:
  name: migrate-pg-1-3-1
spec:
  imagePullSecrets:
  - $patch: delete
    name: auditlog-imagepull-secret
  containers:
  - name: db
    command:
    - node
    - --inspect=0.0.0.0
    - -r
    - ts-node/register
    - src/_db/runner-lite.ts
    - pg
